package edu.umich.med.michr.track;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Basic sanity check provided by the Spring Boot initializer. Its primary purpose is to verify that the Spring application context loads
 * successfully without any configuration issues or bean wiring errors. While it doesn't test any business logic, it ensures that your
 * application can start up properly.
 */
@SpringBootTest
class YhrTrackApplicationTests {
  @Test
  void contextLoads() {
  }
}
